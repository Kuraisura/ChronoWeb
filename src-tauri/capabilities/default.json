{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the main Chrono desktop window",
  "windows": ["main"],
  "permissions": ["core:default", "notification:default"]
}
